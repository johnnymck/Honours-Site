{% include "head.twig" %}
    {% if name is defined %}
    <h1>Welcome back, {{ name }}</h1>
    {% else %}
    <h1>Welcome!</h1>
    {% endif %}
    <p><a href="/login">Login<a></p>
    {{ form | raw }}
{% include "foot.twig" %}